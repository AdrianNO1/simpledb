from setuptools import setup, find_packages

setup(
    name="simpledb",
    version="0.0.1",
    packages=find_packages(),
    install_requires=[
        "requests",
    ],
    url="https://github.com/AdrianNO1/simpledb",
) 